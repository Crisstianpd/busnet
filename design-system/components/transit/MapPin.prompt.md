# MapPin

Teardrop pins for the map. `origin` = your location (green person), `destination` = amber flag, `stop` = Smart Stop (night surface, green ring, circle-dot).

```jsx
<MapPin variant="origin" label="Tú" />
<MapPin variant="destination" label="Hospital Bloom" />
<MapPin variant="stop" />
```
