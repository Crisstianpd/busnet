# Skeleton

Shimmering placeholder for loading content. Stack a few to echo the shape of what's coming (a trip card, a table row).

```jsx
<Skeleton height={20} width="60%" />
<Skeleton height={72} radius="var(--radius-lg)" />
```
