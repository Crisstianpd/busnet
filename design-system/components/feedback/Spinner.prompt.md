# Spinner

Circular indeterminate loader in brand green. Use for short waits (planning a trip, fetching bus positions). For content placeholders prefer `Skeleton`.

```jsx
<Spinner size={20} />
<Spinner size={32} tone="white" />
```
